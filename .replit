language = "python3"
run = "cd backend && pip install -r requirements.txt && python3 -m uvicorn main:app --host 0.0.0.0 --port 8000"

[nix]
channel = "stable-23.05"

[[ports]]
localPort = 8000
externalPort = 80
